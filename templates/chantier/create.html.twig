{% extends 'base.html.twig' %}

{% block title %}Edition Chantier
{% endblock %}

{% block body %}
	
	
		<div class="mt-5">
		<h2 class="text-danger">Création d'un chantier</h2>
		{{form_start(chantier_form)}}
		
		<div class="form-group">
			<div class="">{{form_errors(chantier_form.name)}}</p>
			{{form_widget(chantier_form.name)}}
		</div>

		<div class="form-group mt-3">
			{{form_widget(chantier_form.address)}}
		</div>

		<div class="form-group">
			<div class="">{{form_errors(chantier_form.startsAt)}}</p>
			{{form_widget(chantier_form.startsAt)}}
		</div>

		<div class="form-group">
			<input type="submit" class="btn btn-success mt-3" value="Creer un chantier">
		</div>
		
		{{form_end(chantier_form)}}
		</div>
	
{% endblock %}
